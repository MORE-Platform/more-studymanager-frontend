export interface DataHealthTableItem {
  observationTitle?: string,
  observationId?: number,
  osbervationType?: string,
  start: string,
  timeInfo: string,
  healthState: string,
  upcoming?: boolean
}
